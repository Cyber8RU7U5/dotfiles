alias vi="nvim" # Habit alias
alias agent="eval \$(ssh-agent -c); ssh-add ~/.ssh/github" # Start ssh agent
alias wmi="curl ifconfig.co" # Get public IP
alias top="glances" # Proces monitor
alias calc="galculator" # Calculator
alias update-puppet="~/.config/puppet/update.fish" # Fetch git updates and push to .config
